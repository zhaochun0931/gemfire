# gemfire installation on Ubuntu linux

export GEMFIRE_HOME=/root/vmware-gemfire-9.15.1
export PATH=$GEMFIRE_HOME/bin:$PATH
export CLASSPATH=$GEMFIRE_HOME/lib/geode-dependencies.jar:$GEMFIRE_HOME/lib/geode-core-9.15.1.jar:$CLASSPATH


apt install openjdk-8-jdk

# gfsh version
9.15.1

#









# start locator and server
start locator
start server
list members

# or run the scipt
gfsh run --file demo.gfsh








create region --name=demo --type=REPLICATE_PERSISTENT
list regions



describe region --name=/demo

put --region=demo --key="1" --value="hello_1"

locate entry --key="1" --region=/demo

query --query="select * from /demo"




gfsh -e 'put --region=demo --key="10" --value="hello_10"'

Install the SDK


  sudo apt-get update && \
  sudo apt-get install -y dotnet-sdk-8.0


Install the runtime
  
sudo apt-get update && \
  sudo apt-get install -y aspnetcore-runtime-8.0



Check Installed .NET SDK Version:

dotnet --version


Check Installed .NET Runtime Version:

dotnet --list-runtimes




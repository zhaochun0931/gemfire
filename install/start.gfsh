start locator --name=locator1

start server --name=server1

start server --name=server1 --hostname-for-clients=10.1.1.1

start server --name=server2 --server-port=0

start server --name=server3 --server-port=0



list members


create region --name=exampleRegion1 --type=REPLICATE_PERSISTENT

create region --name=exampleRegion2 --type=PARTITION_REDUNDANT --redundant-copies=2


list regions
